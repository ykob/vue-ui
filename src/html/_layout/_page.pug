block vars

include ../_mixin/_common.pug
include ../_mixin/_svg.pug

-
  var pageTitle = (page.title) ? page.title : 'vue-ui index';

doctype html
html(lang="ja")
  head
    include ./_head.pug
  body
    .l-page
      include ./_gh.pug
      .l-contents
        h1.c-content-header
          |#{pageTitle}
        block content
      include ./_gf.pug
    script(src="/js/vendor.js")
    script(src="/js/main.js")
