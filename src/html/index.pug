extends ./_layout/_page.pug

block vars
  -
    var page = data.pages.index;
    var pages = data.pages;

block content
  ul
    each item in pages
      if (item.title !== '')
        li
          a(href = item.path)
            |#{item.title}
